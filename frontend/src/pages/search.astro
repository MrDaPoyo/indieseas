---
import SearchView from '../components/Search.astro';
import Layout from '../layouts/Layout.astro';

const query = Astro.url.searchParams.get("q") || null;

if (!query || query.trim() === "" || query == null) {
    return Astro.redirect("/");
}
---

<Layout>
	<SearchView />
</Layout>
